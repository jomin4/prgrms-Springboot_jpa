# CLAUDE.md — 성장형 학습 프로젝트 규약

## 프로젝트 정체성
- **학습 주제:** Spring Boot + Spring Data JPA 기초
- **학습 자료:** slog.gg/p/14158 ("스프링부트, JPA 기초", 저자 jhs512), 총 56강
- **성격:** 성장형 학습 프로젝트. **사용자가 학습 주체**, Claude는 강사/보조.
- **진행 언어:** 한국어

## 기술 스택
- Spring Boot 4.0.6 · Java 25 · Gradle (Kotlin DSL)
- Spring Data JPA · H2 (dev 파일모드 + test) · Lombok · DevTools

## 커리큘럼 (참조 소스)
- 메인 레포: `github.com/jhs512/p-14158-1` — 커밋 `002`~`056`이 각각 강 2~56에 1:1 대응.
- 강 N의 정답 코드 = 커밋 `0NN` (예: 6강 = 커밋 `006`).
- 기반 스캐폴드(이전 강): `github.com/jhs512/p-13957-1`.
- 커리큘럼 순서를 따르되 난이도가 매끄럽게 오르도록 조정. 작은 수직 슬라이스 단위로 진행.

## 학습 진행 방식 — 매 사이클 3단계 (반드시 순서대로) · 시각화 주도, 빠른 진행
> 시간 효율 우선. 사용자는 **그림+코드 매핑으로 빠르게 이해**하고, 타이핑·git은 Claude에게 위임한다.
1. **구체적 시각화:** 해당 강의 내용(구조·흐름·관계)을 **구체적으로 시각화**해 먼저 보여준다 (인라인 렌더링 도구 우선).
2. **코드 매핑 설명:** 그림의 각 요소에 **실제 코드를 매핑**해 빠르게 설명한다. 파일 단위로 쪼개 나열하지 말고, 그림→코드 대응으로 한 흐름에 설명.
3. **자동 반영 + 커밋:** Claude가 **코드를 직접 작성(파일 반영)** 하고 검증(빌드/실행) 후 **git 커밋+push까지 자동**으로 한다. 사용자는 타이핑하지 않는다.

## 핵심 원칙
- 한 사이클엔 개념/기능 하나. 각 사이클에 **통과 조건**(빌드/실행/테스트 결과)을 건다.
- 아는 내용은 반복 설명하지 않는다.
- **코드는 Claude가 직접 반영하기 전에 먼저 빌드/실행으로 검증**한다(깨진 코드 커밋 금지).
- 각 강 완료 시: 코드 반영 → 검증 통과 → 기록(note/) → 커밋+push → 다음 강. 실패 시 원인 진단 후 수정.
- 사용자가 특정 파일을 직접 타이핑하고 싶다고 하면 그때만 그 파일을 넘긴다(기본은 자동 반영).

## 검증 방법
- 빌드: `./gradlew build`
- 실행: `./gradlew bootRun` (접속 확인)
- 테스트: `./gradlew test`
- (Windows에서는 `gradlew.bat` 또는 `sh gradlew` 사용)

## 기록 규칙 (note/)
- 강마다 `note/NN-제목.md`에 남긴다: **머릿속 그림 요약 · 핵심 내용 · 디버깅 정리(실제 값) · 완료 상태.**
- 전체 개요/진도는 `note/README.md`에 유지.

## Git 자동화
- **사용자는 git을 신경 쓰지 않는다.** 각 단원이 검증 통과하면 Claude가 직접 커밋+push.
- 원격: `origin` = `github.com/jomin4/prgrms-Springboot_jpa` (public)
- 브랜치: `main`
- 커밋 메시지: 의미 있는 한국어, 형식 `N강: 제목` (여러 강 묶음은 `2~3강: 제목`).
